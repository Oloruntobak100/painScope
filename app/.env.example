# PainScope AI - Environment Variables
# Copy this file to .env and fill in your values

# Supabase (required for auth and data - Phase 2+)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# n8n Webhook - AI Briefing Chat (Phase 3+)
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/chat

# n8n Webhook - Agent Discovery Trigger (Phase 4+)
VITE_N8N_AGENT_WEBHOOK_URL=https://your-n8n-instance.com/webhook/agent

# -----------------------------------------------------------------------------
# Stripe (subscriptions + 7-day trial)
# -----------------------------------------------------------------------------
# Frontend (optional: for Stripe.js / future use; checkout redirect works without it)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Optional: override API base URL when frontend and API run on different origins (e.g. local dev)
# Leave unset in production so /api/create-checkout is same-origin.
# VITE_API_URL=

# -----------------------------------------------------------------------------
# Backend / Vercel serverless (set in Vercel Project Settings → Environment Variables)
# These are used by api/create-checkout.ts and api/stripe-webhook.ts only.
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=sk_test_... or sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_... (from Stripe Dashboard → Webhooks → your endpoint → Signing secret)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key (same as VITE_SUPABASE_ANON_KEY; used to verify user token)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (for webhook to update profiles; keep secret)
# SITE_URL=https://pain-scope.vercel.app (success/cancel redirect base)
#
# Stripe Price IDs (Stripe Dashboard → Products → Prices; copy price_xxx)
# Ranger (Pro): STRIPE_PRICE_RANGER_MONTHLY, STRIPE_PRICE_RANGER_YEARLY
# Commander (Enterprise): STRIPE_PRICE_COMMANDER_MONTHLY, STRIPE_PRICE_COMMANDER_YEARLY
# STRIPE_PRICE_SCOUT_FREE_PLAN=price_xxx (Scout free plan; used by webhook to map to plan free)
# STRIPE_PRICE_RANGER_MONTHLY=price_xxx (7-day trial only on Ranger monthly)
# STRIPE_PRICE_RANGER_YEARLY=price_xxx
# STRIPE_PRICE_COMMANDER_MONTHLY=price_xxx
# STRIPE_PRICE_COMMANDER_YEARLY=price_xxx
